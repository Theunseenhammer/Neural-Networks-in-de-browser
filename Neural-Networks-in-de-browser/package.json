{
  "name": "neural-networks-in-de-browser",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host --port 3000",
    "startservice": "vite build && vite preview --host --port 3000"
  },
  "devDependencies": {
    "vite": "^2.8.0"
  },
  "dependencies": {
    "brain.js": "^2.0.0-beta.4"
  }
}
